<div class="reminder template">
  <section>
    <article>
      {{#with model as |reminder|}}
      <h4 class="reminder-title-individual">
        {{reminder.title}}
      </h4>
      <p>
        {{reminder.date}}
      </p>
      {{/with}}
    </article>
  </section>
  {{outlet}}
</div>
